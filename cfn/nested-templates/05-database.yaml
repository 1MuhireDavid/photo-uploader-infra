AWSTemplateFormatVersion: "2010-09-09"

Parameters:
  ProjectName:
    Type: String
  DbSubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
  RdsSecurityGroupId:
    Type: AWS::EC2::SecurityGroup::Id
  DbInstanceClass:
    Type: String
    Default: db.t3.micro
  DbAllocatedStorage:
    Type: Number
    Default: 20
  DbName:
    Type: String
    Default: photogallery
  DbMasterUsername:
    Type: String
    Default: photoapp
  DbMultiAZ:
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"
  DbPort:
    Type: Number
    Default: 5432

Resources:
# managed relational db, holding one small table
  DbSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup  #tell RDSSubnet it's allowed to place the db instance in
    Properties:
      DBSubnetGroupDescription: !Sub "${ProjectName} private DB subnets (2 AZ) for RDS"
      SubnetIds: !Ref DbSubnetIds
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-db-subnet-group"

  Database:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      Engine: postgres
      DBInstanceClass: !Ref DbInstanceClass
      AllocatedStorage: !Ref DbAllocatedStorage
      StorageType: gp3
      StorageEncrypted: true
      DBName: !Ref DbName
      MasterUsername: !Ref DbMasterUsername
      ManageMasterUserPassword: true
      Port: !Ref DbPort
      DBSubnetGroupName: !Ref DbSubnetGroup
      VPCSecurityGroups:
        - !Ref RdsSecurityGroupId
      MultiAZ: !Ref DbMultiAZ
      PubliclyAccessible: false
      BackupRetentionPeriod: 1
      AutoMinorVersionUpgrade: true #minial backups, automatic patching
      Tags:
        - Key: Name
          Value: !Sub "${ProjectName}-db"

Outputs:
  DbEndpointAddress:
    Value: !GetAtt Database.Endpoint.Address
  DbPort:
    Value: !GetAtt Database.Endpoint.Port
  DbName:
    Value: !Ref DbName
  DbSecretArn:
    Value: !GetAtt Database.MasterUserSecret.SecretArn
